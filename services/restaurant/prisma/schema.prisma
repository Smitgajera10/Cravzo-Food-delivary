generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}


model Restaurant {
  id           String     @id @default(uuid())
  ownerId      String     @unique// from Auth service (User id)
  name         String
  description  String?
  image        String
  address      String
  // city         String
  // state        String
  // country      String
  // postalCode   String
  latitude     Float?
  longitude    Float?
  phone        BigInt
  isVerified   Boolean    @default(false)
  isActive     Boolean    @default(false) // approved by admin
  createdAt    DateTime   @default(now())
  updatedAt    DateTime   @updatedAt

  @@index([ownerId])
  // @@index([city, state])
  @@index([isActive])
}

// model Category {
//   id            String     @id @default(uuid())
//   restaurantId  String
//   name          String
//   description   String?
//   createdAt     DateTime   @default(now())

//   restaurant    Restaurant @relation(fields: [restaurantId], references: [id])
//   menus         Menu[]
// }

// model Menu {
//   id           String     @id @default(uuid())
//   restaurantId String
//   categoryId   String?
//   name         String
//   description  String?
//   price        Float
//   isAvailable  Boolean    @default(true)
//   imageUrl     String?
//   createdAt    DateTime   @default(now())
//   updatedAt    DateTime   @updatedAt

//   restaurant   Restaurant @relation(fields: [restaurantId], references: [id])
//   category     Category?  @relation(fields: [categoryId], references: [id])
//   // orderItems   OrderItem[]

//   @@index([restaurantId])
//   @@index([categoryId])
// }

// model Review {
//   id           String     @id @default(uuid())
//   restaurantId String
//   customerId   String     // from Auth service (User id)
//   rating       Int        @default(5)
//   comment      String?
//   createdAt    DateTime   @default(now())

//   restaurant   Restaurant @relation(fields: [restaurantId], references: [id])
  
//   @@index([restaurantId])
//   @@index([customerId])
// }